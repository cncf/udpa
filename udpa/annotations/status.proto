syntax = "proto3";

package udpa.annotations;

import "google/protobuf/descriptor.proto";

// Magic number in this file derived from top 28bit of SHA256 digest of
// "udpa.annotation.status".
extend google.protobuf.FileOptions {
  StatusAnnotation file_status = 222707719;
}

message StatusAnnotation {
  // The entity is work-in-progress and subject to breaking changes.
  bool work_in_progress = 1;

  // The entity represents the version at which active development for this
  // package should take place. Earlier versions are considered frozen and not
  // eligible for changes, later versions are typically machine generated from
  // this active development version.
  bool active = 2;
}
